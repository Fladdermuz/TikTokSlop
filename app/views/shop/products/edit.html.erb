<div class="mx-auto max-w-2xl space-y-6">
  <div>
    <%= link_to "← Back", shop_product_path(@product), class: "text-sm text-gray-500 hover:text-gray-700" %>
    <h1 class="mt-2 text-2xl font-semibold text-gray-900">Edit <%= @product.name %></h1>
  </div>
  <%= render "form", product: @product %>
</div>
